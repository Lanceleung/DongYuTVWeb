<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="m"
            type="xyz.jdynb.tv.model.LivePlayerModel" />

    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.tencent.smtt.sdk.WebView
            android:background="@color/black"
            android:id="@+id/webview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <LinearLayout
            android:visibility="@{m.showLoading}"
            android:gravity="center"
            android:background="@color/black"
            android:orientation="vertical"
            android:id="@+id/loading_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ProgressBar
                android:max="100"
                android:progress="@{m.progress}"
                android:id="@+id/progress_bar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

            <TextView
                tools:text="正在加载中...(100%)"
                android:textSize="30sp"
                android:text="@{m.progressText}"
                android:textColor="@color/white"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

            <TextView
                android:gravity="center_horizontal"
                android:text="左右方向键换源，长按OK键收藏/取消收藏。\n直播内容为互联网网站内容，本App不提供实际直播服务！"
                android:textColor="@color/white"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
"
        </LinearLayout>

    </FrameLayout>

</layout>

